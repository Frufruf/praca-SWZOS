@*@using NonFactors.Mvc.Grid*@
@model List<SWZOS.Models.Reservations.ReservationsViewModel>
<div class="container">
     <div class="row align-items-center">
        <div class="col-12">
            <div class="form-group">
                <button class="btn btn-primary adminButton" type="button" id="addFootballPitchReservation">Boisko piłkarskie</button>
            </div>
            <div class="form-group">
                <button class="btn btn-primary adminButton" type="button" id="addTenisCourtReservation">Kort tenisowy</button>
            </div>
            <div class="form-group">
                <button class="btn btn-primary adminButton" type="button" id="addBasketballPitchReservation">Boisko do koszykówki</button>
            </div>
            <div class="form-group">
                <button class="btn btn-primary adminButton" type="button" id="addVolleyballPitchReservation">Boisko do siatkówki</button>
            </div>
        </div>
        @*@(Html
            .Grid(Model)
            .Build(columns =>
            {
                columns.Add(model => model.PitchId).Titled("ID boiska");
                columns.Add(model => model.PitchTypeName).Titled("Typ boiska");
                columns.Add(model => model.StartDate).Titled("Czas rozpoczęcia");
                columns.Add(model => model.EndDate).Titled("Czas zakończenia");
                columns.Add(model => model.Price).Titled("Cena");
                columns.Add(model => model.Description).Titled("Opis");
            })
            .Using(GridFilterMode.Header)
            .Empty("Brak rezerwacji")
            .Filterable()
            .Sortable())*@
     </div>
</div>